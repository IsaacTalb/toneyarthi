/**
 * Root-level mounting point for the future mini-player and playback controller.
 * Keeping it outside route screens lets playback survive navigation. Until the
 * playback feature exists, it intentionally contributes no UI or state.
 */
export function PersistentPlayerHost() {
  return null;
}
