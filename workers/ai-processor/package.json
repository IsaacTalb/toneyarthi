{
  "name": "@toneyarthi/ai-processor",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@toneyarthi/ai": "workspace:*",
    "@toneyarthi/prompts": "workspace:*",
    "@toneyarthi/shared": "workspace:*",
    "@toneyarthi/types": "workspace:*"
  },
  "scripts": {
    "dev": "wrangler dev --env development",
    "deploy:dry-run": "wrangler deploy --env development --dry-run --outdir dist",
    "typecheck": "tsc --noEmit",
    "deploy:staging": "wrangler deploy --env staging",
    "deploy:production": "wrangler deploy --env production"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250830.0"
  }
}
